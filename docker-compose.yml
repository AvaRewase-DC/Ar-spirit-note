services:
  app:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      args:
        user: daily-reads
        uid: 1500
    container_name: daily-reads
    restart: unless-stopped
    working_dir: /var/www/Ar-spirit-note
    volumes:
      - ./:/var/www/Ar-spirit-note
      - ./docker/php/custom.ini:/usr/local/etc/php/conf.d/custom.ini
    ports:
      - 1500:9000
    networks:
      - daily-reads
      - external_db_network
    depends_on:
      - db

  # nginx:
  #   container_name: reservation-test-nginx
  #   restart: unless-stopped
  #   image: nginx
  #   ports:
  #     - 1500:80
  #   depends_on:
  #     - app
  #   volumes:
  #     - ./:/var/www/daily-reads
  #     - ./docker/nginx/conf.d/:/etc/nginx/conf.d
  #   networks:
  #     - daily-reads

  db:
    image: mariadb:10.5
    container_name: daily-reads-db
    restart: unless-stopped
    command: --max_allowed_packet=67108864
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
    volumes:
      - mysql-data:/var/lib/mysql
    ports:
      - "1501:3306"
    networks:
      - daily-reads
      - external_db_network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: daily-reads-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      UPLOAD_LIMIT: 256M
    ports:
      - "1502:80"
    depends_on:
      - db
    networks:
      - daily-reads

networks:
  daily-reads:
    driver: bridge
  external_db_network:
    # If you want to use an existing network, set external: true and create it externally
    external: false

volumes:
  mysql-data:
    driver: local
